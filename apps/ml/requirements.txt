fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
opencv-python-headless==4.10.0.84
pytesseract==0.3.13
numpy==1.26.4
pillow==10.4.0
fast-alpr[onnx]==0.4.0
onnxruntime==1.20.1
requests==2.32.3

# --- Optionnel : détection de véhicules deep learning pour le pipeline flux ---
# Décommenter pour activer la classification véhicule par YOLO (classes COCO
# car/truck/bus/motorcycle) à la place de l'heuristique géométrique. Volontaire-
# ment désactivé par défaut : ultralytics tire torch (lourd, inadapté à
# l'hébergement gratuit). Le pipeline retombe automatiquement sur l'heuristique
# si ce paquet est absent.
# ultralytics==8.3.0
